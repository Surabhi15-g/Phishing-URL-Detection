This project is a full-stack, AI-powered web application that detects whether a given URL is safe or phishing.
Users can enter a URL through a web interface, and the backend processes the input using a machine learning model trained on phishing datasets to classify the URL in real time.
